# Lato Travel App - Environment Variables Example
# Copy this file to .env.local and fill in your values

# ============================================
# Server-Side API Configuration (SECURE)
# ============================================
# These variables are ONLY available on the server (Next.js API routes)
# They are NOT exposed to the browser, keeping credentials secure

# External API URL (the actual API your Next.js backend will call)
EXTERNAL_API_URL=https://api.latotravelapp.com

# API Bearer Token (server-side only - NOT exposed to browser)
# This is used by Next.js API routes to authenticate with the external API
API_BEARER_TOKEN=your_bearer_token_here

# Example value (DO NOT use in production):
# API_BEARER_TOKEN=I5XVq3DTeiv7AAxVWOchKw8aV7GVyytP

# ============================================
# Client-Side Configuration (PUBLIC)
# ============================================
# These variables are exposed to the browser (prefixed with NEXT_PUBLIC_)
# Only add non-sensitive configuration here

# Optional: Override the local API base URL if needed (default: /api)
# NEXT_PUBLIC_API_URL=/api

# ============================================
# Google Maps Platform Configuration
# ============================================

# Google Maps API Key - Client-side (PUBLIC)
# This key is exposed to the browser and must be restricted to your domains
# Get this from: https://console.cloud.google.com/apis/credentials
# Restrictions: HTTP referrers + Maps JavaScript API only
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_client_api_key_here

# Google Maps API Key - Server-side (SECURE)
# This key is used in Next.js API routes and never exposed to the browser
# Get this from: https://console.cloud.google.com/apis/credentials
# Restrictions: Places API, Directions API, Geocoding API only
GOOGLE_MAPS_API_KEY_SERVER=your_server_api_key_here

# Example values (DO NOT use in production - create your own keys):
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyC_example_client_key_xxx
# GOOGLE_MAPS_API_KEY_SERVER=AIzaSyB_example_server_key_xxx
